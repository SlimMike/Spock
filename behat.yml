default:
    suites:
        sign:
            contexts:
                - SignContext
                - SignCompareContext
            paths:
                - %paths.base%/features/sign
        turn:
            contexts:
                - TurnContext
            paths:
                - %paths.base%/features/turn
        game:
            contexts:
                - GameContext
            paths:
                - %paths.base%/features/game
        player:
            contexts:
                - PlayerContext
            paths:
                - %paths.base%/features/player